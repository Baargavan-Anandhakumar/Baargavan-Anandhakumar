name: Auto Update Projects
on:
  schedule:
    - cron: "0 0 * * 0"
  workflow_dispatch:

jobs:
  update-projects:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Project Update
        run: echo "Automatically updating projects data..."