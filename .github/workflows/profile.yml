name: Profile Status
on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:

jobs:
  profile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Status Check
        run: echo "Profile workflows are running seamlessly."